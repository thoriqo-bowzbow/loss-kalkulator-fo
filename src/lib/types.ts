/**
 * Tipe domain — re-export dari shared (sumber tunggal web & CLI).
 * Jangan tambahkan logika di sini; perubahan tipe dilakukan di shared/types.d.ts.
 */
export type * from '../../shared/types.js';
